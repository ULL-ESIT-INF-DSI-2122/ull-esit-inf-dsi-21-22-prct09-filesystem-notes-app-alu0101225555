/**
 * Funcion de prueba básica que devuelve la suma entre 2 numeros
 * @param firstNum 
 * @param secondNum 
 * @returns 
 */
export function add(firstNum: number, secondNum: number) {
    return firstNum + secondNum;
  }
  
  /*
  const myConst = 7;
  let myResult = add(1, myConst);
  console.log(`myResult = ${myResult}`);
  */
  